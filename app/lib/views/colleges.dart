import 'package:flutter/material.dart';

class Colleges extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Danh sách các khoa viện"),
      ),
      body: Column(
        children: <Widget>[
          
        ],
      ),
    );
  }
}